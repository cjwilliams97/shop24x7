import { DeliveredOrdersPipe } from './delivered-orders.pipe';

describe('DeliveredOrdersPipe', () => {
  it('create an instance', () => {
    const pipe = new DeliveredOrdersPipe();
    expect(pipe).toBeTruthy();
  });
});
